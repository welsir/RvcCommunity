<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tml.mapper.UserFollowMapper">
    <select id="exist" resultType="boolean" parameterType="java.util.Map">
        SELECT CASE WHEN COUNT(follow_id) >= 1 THEN true ELSE false END AS Result
        FROM rvc_user_follow
        WHERE ${clunme1} = #{value1} AND ${clunme2} = #{value2}
    </select>
</mapper>